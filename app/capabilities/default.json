{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "core:default + минимально необходимые разрешения tauri-plugin-updater (check / download / install) и tauri-plugin-process (relaunch после установки апдейта). Никаких fs/http/shell/dialog плагинов; UI продолжает общаться с backend-ом через invoke зарегистрированных команд (см. main.rs::generate_handler!).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
